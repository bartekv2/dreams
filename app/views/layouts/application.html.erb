<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Kameron:400,700|Unna:700|Zilla+Slab+Highlight:700&display=swap" rel="stylesheet">
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-159138064-1"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'UA-159138064-1');
    </script>
  </head>
  <body class="bg-gray-200 text-gray-800 py-4">
    <div class="container mx-auto px-4">
      <header class="relative mb-8">
        <h1 class="text-3xl md:text-4xl tracking-wider p-0 leading-none" style="font-family: 'Unna-Bold'"><%= link_to get_logo, root_path, class: "text-gray-800" %></h1>
        <span class="text-gray-700 italic text-sm md:text-base absolute -mt-1 md:-mt-2 tracking-wide" style="font-family: 'Unna-Italic'">your online dream diary</span>
        <div class="md:absolute md:top-0 md:right-0 mt-8 md:mt-0">
          <% if user_signed_in? %>
            <%= current_user.username %>
            <%= active_link_to 'Sign out', destroy_user_session_path, method: :delete %>
          <% else %>
            <%= active_link_to 'Log in', new_user_session_path %>
            <%= active_link_to "Sign up", new_user_registration_path %>
          <% end %>
        </div>
        <% if user_signed_in? %>
        <nav class="mt-8 flex justify-center md:justify-start">
          <ul class="flex flex-col lg:flex-row flex-grow text-center lg:text-left">
              <li class="md:mr-2 mb-2 lg:mb-0 max-w-xs"><%= active_link_to 'Create a new dream', new_post_path,  :class => "btn min-w-full" %>
              <li class="md:mr-2 mb-2 lg:mb-0 max-w-xs"><%= active_link_to 'Show all dreams', root_path, active: :exact, :class => "btn min-w-full" %>
              <li class="md:mr-2 mb-2 lg:mb-0 max-w-xs"><%= active_link_to 'Show my dreams only', mydreams_posts_path, :class => "btn min-w-full" %>
              <% if current_user.try(:admin?) %>
                <li class="md:mr-3 max-w-xs"><%= active_link_to 'Show users', users_path, :class => "btn min-w-full" %>
              <% end %>
          </ul>
        </nav>
        <% end %>
      </header>
      <div class="md:flex">
        <main class="flex w-full md:w-2/3">
          <%= yield %>
        </main>
        <aside class="md:w-1/3 md:pl-8 mt-4 md:mt-0 text-right">
          <ul class="mb-1">
            <li class="mb-1">Total Dreams: <%= get_number_of_dreams %></li>
            <li class="mb-1">Total Dreamers: <%= get_number_of_dreamers %></li>
            <li>Private Dreams: <%= get_number_of_private_dreams %></li>
          </ul>
          <%= active_link_to 'About', about_path %>
        </aside>
      </div>
      <footer class="text-center my-8">
        <%= get_footer %>
      </footer>
    </div>
  </body>
</html>
